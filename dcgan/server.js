


function getImagesList() {
    var data = {}

    var experiments = ['squares_adam', 'squares_sgd', 'squares_lion']

    experiments.forEach(function(experiment) {
        // Iterate over all files in the 
    })

    return JSON.stringify(data)
}